version: '3.8'

services:
  dashboard:
    image: ghcr.io/techtonic-plates-blog/dashboard:nightly
    env_file:
      - .dashboard.env
    ports:
      - "3050:3000"
    networks:
      - techtonic_plates_network

networks:
  techtonic_plates_network:
    external: true